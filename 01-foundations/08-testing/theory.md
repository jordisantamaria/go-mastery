# 08 - Testing

> Modulo en progreso. Temas a cubrir:

- `go test` basico
- Table-driven tests (el patron estandar en Go)
- Subtests con `t.Run`
- Test fixtures y helpers (`t.Helper()`)
- Benchmarks (`testing.B`)
- Fuzzing (`testing.F`)
- Mocking con interfaces (sin frameworks)
- Test coverage (`go test -cover`)
- Integration tests con build tags
- testify (assertions populares)
- Preguntas de entrevista sobre testing
